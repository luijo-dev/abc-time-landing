<header class="vitrine-header">
  <div class="vitrine-header__inner">
    <a class="vitrine-header__brand" href="/" aria-label="AVC TIME">
      <span class="vitrine-header__eyebrow">Alta relojería</span>
      <span class="vitrine-header__name">AVC TIME</span>
    </a>
  </div>
</header>

<style>
  .vitrine-header {
    position: sticky;
    top: 0;
    z-index: 20;
    padding: var(--space-16) var(--space-24) 0;
  }

  .vitrine-header__inner {
    width: min(1120px, 100%);
    margin: 0 auto;
    padding: var(--space-16) var(--space-24);
    border: 1px solid rgb(var(--color-outline-variant-rgb) / 0.12);
    border-radius: var(--radius-lg);
    background: rgb(var(--color-surface-variant-rgb) / 0.72);
    backdrop-filter: blur(var(--blur-glass));
    -webkit-backdrop-filter: blur(var(--blur-glass));
    box-shadow: var(--shadow-ambient);
  }

  .vitrine-header__brand {
    display: inline-grid;
    gap: var(--space-4);
  }

  .vitrine-header__eyebrow {
    color: var(--color-on-surface-variant);
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: var(--letter-spacing-label);
    text-transform: uppercase;
  }

  .vitrine-header__name {
    color: var(--color-on-surface);
    font-family: var(--font-display);
    font-size: clamp(1.625rem, 4vw, 2.125rem);
    font-weight: 600;
    letter-spacing: 0.08em;
    line-height: 1;
    text-transform: uppercase;
  }

  @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
    .vitrine-header__inner {
      background: rgb(var(--color-surface-variant-rgb) / 0.96);
    }
  }

  @media (max-width: 720px) {
    .vitrine-header {
      padding-inline: var(--space-16);
    }

    .vitrine-header__inner {
      padding-inline: var(--space-16);
    }
  }
 </style>
